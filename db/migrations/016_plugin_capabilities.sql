-- ============================================================
-- 迁移 016：插件能力登记列（P2 安全加固：运行时能力门控取交集）
-- 说明：安装/升级时把市场清单（或包内 manifest）声明的 capabilities 落库；
--       进程启动时以「登记能力 ∩ 二进制自报能力」做门控——恶意二进制自报
--       data.read 不再直接获得数据服务（需安装登记中已声明）。
--       存量记录默认 '[]'（无登记=无扩展能力，收紧策略；重装/升级后刷新）。
-- ============================================================

ALTER TABLE plugin_instances ADD COLUMN IF NOT EXISTS capabilities JSONB NOT NULL DEFAULT '[]';
